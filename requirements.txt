# Core dependencies
python-dotenv>=1.0.0
pydantic>=2.0.0
fastapi>=0.104.0
uvicorn>=0.24.0
requests>=2.31.0
python-multipart>=0.0.6

# Logging and utilities
loguru>=0.7.0

# Development dependencies
pytest>=7.4.0
black>=23.0.0
flake8>=6.0.0
mypy>=1.6.0
pre-commit>=3.5.0

# Optional: Add mode-specific dependencies as needed
# For external AI mode: openai>=1.0.0 (if using OpenAI-compatible API)
# For self-hosted mode: chromadb, ollama, beautifulsoup4, etc.