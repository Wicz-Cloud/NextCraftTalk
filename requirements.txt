# Core dependencies
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
fastapi>=0.120.1  # Security: Compatible with Starlette 0.49.1+
starlette>=0.49.1  # Security: Fix O(n^2) DoS in FileResponse Range header merging
uvicorn>=0.24.0
requests>=2.33.0
python-multipart>=0.0.22  # Security: fix arbitrary file write (non-default config)

# Logging and utilities
loguru>=0.7.0

# Development dependencies
pytest>=7.4.0
black>=23.0.0
flake8>=6.0.0
mypy>=1.6.0
pre-commit>=3.5.0

# Optional: Add mode-specific dependencies as needed
# For external AI mode: openai>=1.0.0 (if using OpenAI-compatible API)
# For self-hosted mode:
chromadb==1.4.0
sentence-transformers==5.3.0
beautifulsoup4>=4.12.0
lxml>=4.9.0
requests>=2.33.0

# Security: pin vulnerable transitive dependencies
pillow>=12.1.1  # Fix out-of-bounds write on PSD images
protobuf>=6.33.5  # Fix JSON recursion depth bypass
pyasn1>=0.6.2  # Fix DoS in decoder
virtualenv>=20.36.1  # Fix TOCTOU directory creation
filelock>=3.20.3  # Fix TOCTOU symlink attacks

# Force urllib3 update for security
urllib3>=2.6.3  # Fix decompression bomb + redirect chain vulnerabilities
