# Core dependencies for Self-Hosted mode
python-dotenv>=1.0.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
fastapi>=0.120.1  # Security: Compatible with Starlette 0.49.1+
starlette>=0.49.1  # Security: Fix O(n^2) DoS in FileResponse Range header merging
uvicorn>=0.24.0
requests>=2.32.0
python-multipart>=0.0.6

# Logging and utilities
loguru>=0.7.2
watchdog>=3.0.0

# Self-hosted specific dependencies
chromadb==1.3.4
sentence-transformers==5.1.2
beautifulsoup4>=4.12.0
lxml>=4.9.0

# Force urllib3 update for security - monitoring for CVE-2025-50181 and CVE-2025-50182 fixes
urllib3>=2.5.0

# Development dependencies (optional for production)
pytest>=8.0.0
black>=24.0.0
flake8>=7.0.0
mypy>=1.8.0
pre-commit>=3.6.0
